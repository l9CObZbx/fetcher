{
  "eval_id": 2,
  "eval_name": "function-calling",
  "prompt": "Create a script that uses function calling with the OpenAI client. Define a get_weather function tool with location parameter, send a user message asking 'What's the weather in San Francisco?', and handle the tool call by extracting the function name and arguments. Print out which tool was called and the parsed arguments.",
  "assertions": [
    "Creates a .ts or .js file that imports OpenAI from @ahoo-wang/fetcher-openai",
    "Defines a tools array with a function tool of type 'function'",
    "Tool has name 'get_weather' and location parameter with description",
    "Sends chat.completions() with tools and tool_choice options",
    "Checks response.choices[0].message.tool_calls to detect tool call",
    "Extracts and prints function name and parsed arguments from tool_calls[0]"
  ]
}
