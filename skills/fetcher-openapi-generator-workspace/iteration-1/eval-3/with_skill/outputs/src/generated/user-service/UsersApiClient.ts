import { type ApiMetadata, type ApiMetadataCapable, type ParameterRequest, api, attribute, autoGeneratedError, get, path, request } from "@ahoo-wang/fetcher-decorator";
import { UserState } from "./types";

@api()
export class UsersApiClient implements ApiMetadataCapable {
    constructor(public readonly apiMetadata?: ApiMetadata) {
    }

    /**
     * Get user by ID
     * - operationId: `getUser`
     * - path: `/tenant/{tenantId}/users/{userId}`
     */
    @get('/tenant/{tenantId}/users/{userId}')
    getUser(@path('userId') userId: string, @request() httpRequest?: ParameterRequest, @attribute() attributes?: Record<string, any>): Promise<UserState> {
        throw autoGeneratedError(userId, httpRequest, attributes);
    }
}
